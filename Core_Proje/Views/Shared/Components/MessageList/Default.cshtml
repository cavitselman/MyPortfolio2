@using Core_Proje.EL.Concrete
@model List<Message>

<div class="col-md-6 col-xl-4 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="d-flex flex-row justify-content-between">
                <h4 class="card-title">Mesajlar</h4>
                <a href="/Contact/Index/"><p class="text-muted mb-1 small">Tümünü Görüntüle</p></a>
            </div>
            @for (int i = 0; i < Model.Count; i++)
            {
                var item = Model[i];
                var imageUrl = ((List<string>)ViewBag.ImageUrl)[i]; // Her mesaj için ilgili ImageUrl

                <div class="preview-list">
                    <div class="preview-item border-bottom">
                        <div class="preview-thumbnail">
                            @if (!string.IsNullOrEmpty(imageUrl))
                            {
                                <img src="~/UserImage/@imageUrl" alt="image" class="rounded-circle" />
                            }
                            else
                            {
                                <img src="~/UserImage/default.png" alt="Default Image" class="rounded-circle" /> <!-- Varsayılan görsel -->
                            }
                        </div>
                        <div class="preview-item-content d-flex flex-grow">
                            <div class="flex-grow">
                                <div class="d-flex d-md-block d-xl-flex justify-content-between">
                                    <h6 class="preview-subject">@item.Name</h6>
                                    <p class="text-muted text-small">@(((DateTime)item.Date).ToString("dd-MMM-yyyy"))</p>
                                </div>
                                <p class="text-muted">@item.Content.Substring(0, 30)...</p>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
